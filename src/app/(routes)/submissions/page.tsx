import SubmissionsContainer from "@/containers/Submissions";

const PromptListingPage = ({ searchParams }: any) => {
  return <SubmissionsContainer params={searchParams} />;
};

export default PromptListingPage;
